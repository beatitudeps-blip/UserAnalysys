{
  "name": "price-investor",
  "version": "1.0.0",
  "description": "家電ECサイト価格比較ツール（Playwright）",
  "main": "src/price-research.js",
  "scripts": {
    "start": "node src/price-research.js",
    "local": "node src/price-research.js",
    "local:setup": "npm install && npx playwright install chromium",
    "local:full": "npm install && npx playwright install chromium && node src/price-research.js",
    "test:browser": "node src/browser-test.js",
    "setup": "npx playwright install chromium",
    "debug:kakaku": "node src/debug-kakaku.js",
    "debug:item": "node src/debug-item.js"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
